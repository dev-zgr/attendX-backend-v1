server:
  port: 8080

spring:
  application:
    name: "AttendX"
    datasource:
        url: jdbc:mysql://localhost:3306/attendX
        username: root
        password: root
        driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create


info:
  app:
    name: "AttendX Service "
    description: "Microservice to manage attendX endpoints"
    version: "1.0.0"